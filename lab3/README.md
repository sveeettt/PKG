# Image Processing Application

## Описание

Графическое приложение для обработки изображений с использованием различных методов пороговой обработки и сегментации. Программа предоставляет инструменты для анализа и преобразования изображений с возможностью настройки параметров обработки в реальном времени.

## Основные функции

### 🔧 Методы обработки изображений

- **Глобальная пороговая обработка (метод Оцу)** - автоматическое определение оптимального порога
- **Глобальная пороговая обработка (итеративный метод)** - ручная настройка порогового значения
- **Адаптивная пороговая обработка** - локальная обработка с учетом окружающих пикселей
- **Обнаружение границ (оператор Собеля)** - выделение контуров объектов
- **Обнаружение линий (преобразование Хафа)** - обнаружение прямых линий на изображении
- **Обнаружение точек** - идентификация изолированных точек и особенностей

### 🖼️ Тестовые изображения

Встроенные тестовые изображения для проверки алгоритмов:
- Зашумленное изображение
- Размытое изображение
- Малоконтрастное изображение
- Темное изображение
- Светлое изображение

### ⚙️ Настраиваемые параметры

- Пороговое значение (0-255)
- Размер ядра для адаптивной обработки (3-15)
- Преобразование в оттенки серого
- Режим реального времени при изменении параметров

## Установка и сборка

### Требования

- Qt 5.15 или выше
- Компилятор C++ с поддержкой C++11
- CMake (опционально)

### Сборка из исходного кода

```bash
# Клонирование репозитория
git clone <repository-url>
cd <project-directory>

# Сборка через qmake
qmake
make

# Или через CMake
mkdir build
cd build
cmake ..
make
```

### Открытие в Qt Creator

1. Запустите Qt Creator
2. Откройте проект через `File → Open File or Project`
3. Выберите файл `.pro`
4. Нажмите `Configure Project`
5. Соберите проект (`Build → Build Project`)

## Использование

### Основные операции

1. **Загрузка изображения**
   - Нажмите "Загрузить изображение" для выбора файла
   - Поддерживаемые форматы: PNG, JPG, JPEG, BMP, TIFF

2. **Выбор тестового изображения**
   - Используйте выпадающий список для выбора предустановленных тестовых изображений

3. **Настройка параметров обработки**
   - Выберите метод обработки из выпадающего списка
   - Настройте параметры с помощью ползунков и спинбоксов
   - Включите/отключите преобразование в оттенки серого

4. **Обработка изображения**
   - Нажмите "Обработать" для применения выбранного алгоритма
   - Изменения параметров в реальном времени автоматически обновляют результат

5. **Сохранение результата**
   - Нажмите "Сохранить результат" для экспорта обработанного изображения

### Алгоритмы обработки

#### Глобальная пороговая обработка (Оцу)
Автоматически вычисляет оптимальный порог для бинаризации изображения на основе гистограммы яркостей.

#### Глобальная пороговая обработка (Итеративная)
Позволяет вручную установить пороговое значение с возможностью итеративного уточнения.

#### Адаптивная пороговая обработка
Применяет различные пороги для разных областей изображения на основе локальных характеристик.

#### Обнаружение границ (Собеля)
Выделяет границы объектов с использованием оператора Собеля для вычисления градиента.

#### Обнаружение линий (Хафа)
Обнаруживает прямые линии на изображении с использованием преобразования Хафа.

#### Обнаружение точек
Идентифицирует изолированные точки и особенности на основе локальных максимумов.

## Структура проекта

```
project/
├── mainwindow.h          # Заголовочный файл главного окна
├── mainwindow.cpp        # Реализация методов главного окна
├── main.cpp             # Точка входа приложения
├── ImageProcessor.pro   # Файл проекта Qt
└── README.md           # Документация
```

## Классы и методы

### MainWindow

Основной класс приложения, содержащий:

- **Элементы интерфейса**: метки изображений, кнопки, комбобоксы, слайдеры
- **Методы обработки**: алгоритмы пороговой обработки и сегментации
- **Утилиты**: преобразование цветовых пространств, создание тестовых изображений

### Основные методы

- `applyGlobalThresholdOtsu()` - метод Оцу для глобальной пороговой обработки
- `applyGlobalThresholdIterative()` - итеративная пороговая обработка
- `applyAdaptiveThreshold()` - адаптивная пороговая обработка
- `detectEdgesSobel()` - обнаружение границ оператором Собеля
- `detectLinesHough()` - обнаружение линий преобразованием Хафа
- `detectPoints()` - обнаружение точек
- `convertToGrayscale()` - преобразование в оттенки серого

## Примеры использования

### Бинаризация документа

1. Загрузите изображение документа
2. Выберите "Глобальная пороговая (Оцу)"
3. Нажмите "Обработать" для четкого разделения текста и фона

### Обнаружение контуров

1. Загрузите изображение с объектами
2. Выберите "Обнаружение границ (Собеля)"
3. Настройте параметры для оптимального выделения контуров

### Анализа структуры изображения

1. Используйте тестовое изображение с линиями и точками
2. Примените соответствующие алгоритмы обнаружения
3. Проанализируйте результаты сегментации


